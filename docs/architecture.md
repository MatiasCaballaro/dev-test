# Architecture

The test repository starts with a minimal application file, a README, and this architecture document.

Incremental ingestion should later detect changes to individual paths without reprocessing unchanged files.
