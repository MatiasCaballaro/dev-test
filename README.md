# Dev Test

Small repository used to validate incremental ingestion by commit and file diff.

## Baseline

This first version contains three ingestable files so the initial run can be verified as a full ingest.

This line validates automatic read-side artifact promotion after the V1-10 manual fix.

This line validates pre-admission verify detecting a newer remote commit.
